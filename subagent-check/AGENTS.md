# subagent-check

- サブエージェントの起動そのものは呼び出し元スキルに任せ、このスキルは起動前の状態確認に責務を限定する。
- 完了済みサブエージェントがある場合は、実行環境で利用できる結果取得操作で結果を回収し、要点保持と close 判断を先に行う。
- 起動可否は `OK` / `REUSE` / `WAIT` / `ASK_USER` のいずれかで返し、呼び出し元が次に取る行動を明確にする。
