# subagent-review

## 前提ツール

- [git](https://git-scm.com/)

## 責務の境界

- 同セッションの general-purpose サブエージェントへ差分レビューを委譲することに責務を限定する
- 起動前確認は `/subagent-check`、結果通過時のタグ設置は `/mark review-sub` へ委譲する
- レビュー指摘の最終判断（修正する／対応不要）と整理・報告はメインエージェントが担う。サブエージェントの役割は差分レビューの実行に限定する
