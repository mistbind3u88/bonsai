## タスク管理・ドキュメント

- 作業の開始時にそのディレクトリがworktreeであるなら、リポジトリの元ディレクトリの `.claude/docs` 配下に関連したタスクドキュメントがあるかを確かめる
  - タスクドキュメントを新しく追加するときは原則として、worktreeの元ディレクトリの `.claude/docs` 配下に追加する
- compacting終了後、リポジトリローカルの `.claude` 配下に実行中タスクに関するドキュメントがあったら作業を再開する前に必ず読み込む
