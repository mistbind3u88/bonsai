# AGENTS.md

## このリポジトリの目的

特定のプロジェクトや業務領域に依存しない、汎用的な開発スキル群（L0/L1/L2）を管理する。

## スキルの追加・変更に関する制約

### 含めてよいもの

- git / GitHubの操作に関するスキル
- Claude Codeのセットアップや設定に関するスキル
- コードレビュー、リント、フォーマットなど汎用的な開発プロセスのスキル
- 言語やフレームワークに依存しない汎用ツール連携のスキル

### 含めてはいけないもの

- **業務領域固有の知識やロジック** — 特定プロジェクトのドメインモデル、ビジネスルール、社内用語への依存
- **秘匿情報** — APIキー、トークン、認証情報、社内URL、内部システム名
- **特定組織のインフラ情報** — CI/CDパイプラインの詳細設定、デプロイ先のホスト名、内部ネットワーク構成
- **特定リポジトリの構造に依存するスキル** — 特定のディレクトリ構成やファイル命名規則を前提とするもの

### 判断基準

スキルを追加・変更する際は以下を確認すること:

1. **公開リポジトリである** — pushされる内容はすべて公開される前提で書く
2. **汎用性** — git/GitHubを使う任意のプロジェクトで利用できるか
3. **独立性** — 特定のリポジトリをクローンしなくても単体で動作するか
4. **最小権限** — `allowed-tools` はスキルの目的に必要なコマンドとサブコマンドに限定する。包括的な許可 (例: `Bash(npm:*)`) ではなく、実行するサブコマンド単位 (例: `Bash(npm run:*)`, `Bash(npm test:*)`) で指定する。他のスキルに委譲できる操作は自スキルの `allowed-tools` に含めず、`/skill-name` 呼び出しで委譲する。スキル更新時には不要になった許可を必ず削除する。スキル配下に実装しているスクリプトやツールは `allowed-tools` に含めない（`Bash($SKILL_DIR/foo.sh:*)` のようにスキル相対パスで書いても Claude Code が argv[0] を runtime-determined として毎回承認を要求するため機能しない）。該当スクリプトは PATH 上に配置し bare name で呼び出す前提とする。skill 配下に実行用 script/tool を追加した場合は、リポジトリ直下の `.tools` に同名のシンボリックリンクを作成し、skill の手順では `.tools` が PATH に含まれる前提で bare command として呼び出す。`allowed-tools` には skill 内部パスを直接書かず、`.tools` 経由で PATH に載るコマンド名を指定する
5. **MECE** — スキル間で機能の重複や漏れがないよう設計する。既存スキルと責務が重なる場合は、既存スキルの拡張や呼び分けで対応し、新規スキルの追加は避ける
6. **循環参照禁止** — スキル間の `/skill-name` 呼び出しが循環しないことを確認する。作成・更新時には呼び出し先を辿って循環がないかチェックする
7. **ファイル命名規則** — スキル定義ファイルは `SKILL.md`、エージェント向け指示ファイルは `AGENTS.md` とし、常に大文字で命名する。`AGENTS.md` を配置する場合は `CLAUDE.md` → `AGENTS.md` のシンボリックリンクを必ずセットで作成する
8. **タスクドキュメント配置** — 他エージェントや将来対応のためのタスクドキュメントは、worktree 側ではなく元リポジトリの `.claude/docs` に配置する。参照は `/read-taskdoc`、作成・更新は `/edit-taskdoc` を使う。worktree 側の `.claude/docs` に同種のドキュメントを作成・更新してはならない。削除や恒久ドキュメントへの移管は `/clean-docs` に委譲する
9. **肯定形の手順記述** — スキルや関連ドキュメントを更新する際は、禁止事項の列挙ではなく、各場面で実行者が取るべき行動を肯定形で明記する。例外や対象外を示す場合も「何をしないか」だけで終えず、「代わりにどのスキルへ委譲するか」「どのファイルへ反映するか」「どの確認を行うか」まで書く
10. **サブエージェント起動前確認** — スキルでサブエージェントを起動する場合は、起動前に現在のサブエージェント一覧、完了状態、役割、起動上限に対する余剰を確認する。役割を終えたサブエージェントがあれば結果を回収し、不要なものを閉じて枠を空ける。余剰がある場合にだけ新しいサブエージェントを起動する。余剰がない場合は、既存サブエージェントの再利用、完了待ち、またはユーザー確認のいずれかを選び、どの判断をしたかを報告する
11. **サブエージェント利用境界** — サブエージェントを使うスキルでは、スキルの起動、手順解釈、`/subagent-check` 実行、結果回収、最終判断、`mark`、ユーザー報告をメインエージェントが担う。サブエージェントには、検査・文面確認・想定質問への回答など、明示された限定タスクだけを依頼する。依頼文では、メインエージェントが実行中のスキル名、サブエージェントの役割、返すべき結果、メイン側に残す判断を具体的に書く

## スキルの分類

スキルは依存関係の構造に応じて 3 層に分類する。各層は MECE のスコープが異なる。

### L0 atomic（他スキルへ委譲しない単機能）

他のスキルへ `/<skill>` を介した処理委譲を行わず、自身の操作だけで責務を完結させるスキル。MECE の対象であり、スキル間で機能が重複しないように設計する。`allowed-tools` は必要最小限。

役割の細分:

- **utility**: 共通基盤として他スキルから呼ばれる（例: `mark`, `subagent-check`, `backup-branch`）
- **atomic**: 独立した本体機能（例: `tanaoroshi`, `monthly-report`, `link-skills`, `watch-ci`, `reply-review`, `issue-review`, `collect-feedback`）

### L1 composite（自身の操作 + 他スキルへの委譲を持つ単機能）

自身でも具体的な操作を行いつつ、責務の一部を他スキルへ委譲するスキル。MECE の対象だが、委譲先との境界をスキル本文で明示する必要がある。

役割の細分:

- **read**: 情報を参照・取得する（例: `gh-read`, `read-taskdoc`）
- **edit**: ファイルや GitHub の状態を変更する（例: `commit`, `fixup`, `gh-edit`, `edit-taskdoc`, `doc-sync`, `wiki-sync`, `clean-docs`）
- **check**: 検証・レビューを行い結果を返す（例: `check`, `doc-check`, `codex-review`, `claude-review`）
- **history**: git 履歴・PR の進行に対する操作（例: `catch-up`, `push`, `pr-progress`）
- **bootstrap**: 作業開始・引き継ぎの準備（例: `start-dev`, `takeover`）

### L2 workflow（オーケストレーション専用）

複数のスキルを決まった順序で呼び出すラッパー。自身では操作を行わず、`allowed-tools` は空にする。

- L2 は MECE の対象外。組み合わせを定義するものであり、機能の重複ではない
- L0/L1 のスキルが L2 を呼び出してはならない（循環参照の原因になる）
- いずれかのステップが失敗したらワークフロー全体を停止する

例: `ship`（代表ステップ: `/commit` → `/check` → `/push` → `/gh-edit` → `/watch-ci` → `/pr-progress`）、`respond`

### 将来の方向性

L1 は「自分の操作 + 委譲」を併せ持つため境界が曖昧になりやすい。長期的には L1 を薄くし、責務を **L0（純粋な単機能）と L2（オーケストレーション）** に寄せる方針を取る。新規スキルを追加する際は、L1 として作るより、L0 への分解 + L2 への組み合わせで表現できないかを先に検討する。既存の L1 スキルも、再設計の機会があれば L0 への分解を優先する。

## 品質チェック

- lint: `prettier --check .`
- shellcheck: `shellcheck **/*.sh`
